//import uniqid from "uniqid";

class Card {
    constructor(id, image, title) {
        this.id = id; // TODO: Set to uniqid() if id is undefined
        this.image = image;
        this.title = title;
    }
}

export default Card;
